import { movieGenre } from 'viedor/helpers/movie-genre';
import { module, test } from 'qunit';

module('Unit | Helper | movie genre');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = movieGenre([42]);
  assert.ok(result);
});
